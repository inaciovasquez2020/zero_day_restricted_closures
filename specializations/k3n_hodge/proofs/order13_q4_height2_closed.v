Standalone parent closure of the homogeneous q=4, height-two order-13
deviation-two branch.

SCOPE:
  Retain

    S := C[x1,x2,x3,x4],
    I := Q+(f,g),
    A := S/I,
    length_C(A)=N>=32,

  where Q is minimally generated by exactly four independent quadrics,

    ht(Q)=2,
    deg(f),deg(g)>=3.

  Put

    Qsat := saturation(Q),
    C := S/Qsat.

This file performs one bounded task only: combine the already-established
height-two child classifications.  It does not enter height three, q<=3, or
Oblivion Closure.

This is pseudo-formal mathematical documentation.  It is not Coq or Lean and
MACHINE_VERIFICATION_OF_NEW_THEOREM is not claimed.

--------------------------------------------------------------------------
1. EXHAUST MULTIPLICITY AND DEPTH
--------------------------------------------------------------------------

The saturated-core profile proves

    dim C=2,
    depth_m(C) in {1,2},
    e(C) in {1,2,3}.

The multiplicity-three exclusion proves

    e(C) != 3.

Hence every survivor has

    e(C) in {1,2}.

--------------------------------------------------------------------------
2. DEPTH TWO
--------------------------------------------------------------------------

Assume depth_m(C)=2.

The exact Cohen--Macaulay core inventory leaves only

  LINE CORE:
    Qsat=(l1,l2),
    e(C)=1,

  PLANE CONIC CORE:
    Qsat=(l,q),
    deg(q)=2,
    e(C)=2.

The plane-cubic multiplicity-three possibility is impossible.

The line core is tangent-closed by

    q4_height2_CM_line_core_tangent_closed.

The plane-conic core is tangent-closed by

    q4_height2_CM_conic_subbranch_is_closed.

Therefore no depth-two height-two state survives.

--------------------------------------------------------------------------
3. DEPTH ONE, MULTIPLICITY ONE
--------------------------------------------------------------------------

Assume

    depth_m(C)=1,
    e(C)=1.

The exact four-state residual Hilbert frontier is

    H00, H01, H10, H11.

Those states are respectively tangent-closed, closed, tangent-closed, and
empty.  Therefore

    q4_height2_multiplicity_one_depth_one_closed.

Hence no depth-one multiplicity-one state survives.

--------------------------------------------------------------------------
4. DEPTH ONE, MULTIPLICITY TWO
--------------------------------------------------------------------------

Assume

    depth_m(C)=1,
    e(C)=2.

The exact multiplicity-two four-quadric classification leaves Types A1, A2,
B, and C.

  Type A1 is the skew-line endpoint and is tangent-closed.

  Type A2 is the saturated double-line endpoint and is tangent-closed.

  Type B is the punctured-spectrum-defective endpoint and is tangent-closed.

  Type C saturates to the Cohen--Macaulay plane-conic branch, so it cannot
  remain in depth one and its tangent branch is already closed in depth two.

The hypothetical finite Rao length-two stratum is empty and introduces no
additional type.

Therefore no depth-one multiplicity-two state survives.

--------------------------------------------------------------------------
5. HEIGHT-TWO PARENT CLOSURE
--------------------------------------------------------------------------

Theorem q4_height2_closed:
  No homogeneous order-13 deviation-two candidate with exactly four quadratic
  minimal generators and ht(Q)=2 can pass the necessary tangent-deficit gate.

Proof:
  The saturated-core profile gives depth(C) in {1,2} and e(C) in {1,2,3}.

  Multiplicity three is impossible.

  If depth(C)=2, the exact CM inventory is line or conic, and both are closed.

  If depth(C)=1 and e(C)=1, the multiplicity-one parent is closed.

  If depth(C)=1 and e(C)=2, the exact multiplicity-two classification is
  exhausted by A1, A2, B, and C, all excluded as above.

  Hence every height-two state is eliminated.
Qed.

RESULT:
  q4_height2_closed.

DEPENDENCIES:
  q4_height2_saturated_core_profile.
  q4_height2_multiplicity_three_is_impossible.
  q4_height2_CM_line_core_tangent_closed.
  q4_height2_CM_conic_subbranch_is_closed.
  q4_height2_multiplicity_one_depth_one_closed.
  q4_height2_depth_one_skew_line_endpoint_is_closed.
  q4_height2_depth_one_double_line_endpoint_is_closed.
  q4_height2_depth_one_TypeB_profile_is_closed.
  q4_height2_depth_one_finite_length_two_Rao_stratum_is_empty.

IMPORTANT_NONCONCLUSION:
  This file closes only the homogeneous q=4, ht(Q)=2 branch.

  It does NOT close q=4 height three.
  It does NOT close homogeneous q<=3.
  It does NOT close the unrestricted nonhomogeneous local deviation-two
  frontier.
  It does NOT promote Oblivion Closure.

PROOF_STATUS:
  pseudo-formal mathematical documentation.

MACHINE_VERIFICATION_OF_NEW_THEOREM:
  not claimed.

BOUNDARY:
  q4_height2_closed.
  not q4_full_closure.
  not homogeneous_q_le_3_closure.
  not OC.

NEXT_ACTIONS:
  1. Re-read the exact terminal workflow for this commit.
  2. If green, inspect only the q=4 height-three terminal frontier and identify
     its first surviving h-vector before any q=4 parent promotion.
